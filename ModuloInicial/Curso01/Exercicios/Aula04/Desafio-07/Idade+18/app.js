let idade = prompt ("Quantos anos você tem?");

if(idade < 18) {
    console.log (`Você tem ${idade} portando você é MENOR de idade!`);
    alert (`Você tem ${idade} portando você é MENOR de idade!`);

} else {
    console.log (`Você tem ${idade} portando você é MAIOR de idade!`);
    alert (`Você tem ${idade} portando você é MAIOR de idade!`);
}