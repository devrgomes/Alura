let num1 = parseInt(prompt("Digite um número inteiro:"));
let num2 = parseInt(prompt("Digite outro número inteiro:"));
let resultado = num1 - num2;

alert (`A diferença entre ${num1} e ${num2} é igual a ${resultado}.`);
console.log (`A diferença entre ${num1} e ${num2} é igual a ${resultado}.`);