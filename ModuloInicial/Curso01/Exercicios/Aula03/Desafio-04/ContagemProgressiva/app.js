let contInicial = 0;
let contador = prompt ("Digite um número para contarmos de 0 até ele:");

while (contInicial <= contador) {
    console.log (`Contagem progressiva: ${contInicial}`);
    contInicial++;
}